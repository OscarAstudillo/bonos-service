apiVersion: v1
kind: Service
metadata:
  name: bonos-service
spec:
  type: ClusterIP  # ClusterIP significa que es 100% privado, no sale a internet
  selector:
    app: bonos-service
  ports:
  - port: 8004
    targetPort: 8004